# SecureDrop frontend

React + TypeScript + Vite interface for the SecureDrop Rust backend.

Development:

```powershell
npm install
npm run dev
```

Production build:

```powershell
npm run build
```

Vite proxies `/api` to `http://127.0.0.1:3000` during development.
